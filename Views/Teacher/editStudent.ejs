<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student Record</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

  
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/notification.js" defer>
        
    </script>
    <script>
        // Check if viewData is defined before parsing
        <% if (typeof viewData !== 'undefined') { %>
            let viewData = JSON.parse('<%- JSON.stringify(viewData) %>');
        <% }%>
        console.log(viewData);
    </script>
    <style>
        
        body {
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .form{
            margin-top: -200px;
        }
        .form-container {
            border: 2px solid #34495e;
            background-color: #f5f5f5;
            padding: 20px 40px 40px;
            border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.192);
            width: 600px; 
            transition: all 0.3s ease-in-out; 
        }
        .form-container:hover {
            transform: scale(1.05); 
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.3);
        }
        .form-container h1 {
            color: #343a40;
        }
        .form-group {
            margin-bottom: 25px; 
        }
        .form-control {
            border-radius: 10px; 
        }
      
        .btn-primary:hover {
            background-color: #293b4a; 
        }
    </style>
    
     
</head>
<body>
    <%- include("../partials/teacherSideBar.ejs") %>

    <div class="container">
        <form action="/teacher/dashboard/student/<%- viewData.student.id %>/edit" method="post" style="margin-top: -200px;" class="text-center" >
            <div class="form-container">
                <h1 class="mb-4">Edit Student Record</h1>
                <div class="form-group">
                    <input type="number" class="form-control" placeholder="Roll No." name="rollNo" id="rollNo" readonly value="<%- viewData.student.rollNo %>" >
                </div>
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Name" name="name" id="name" value="<%- viewData.student.name %>">
                </div>
                <div class="form-group">
                    <input type="date" name="dob" class="form-control" id="dob" required  value="<%- viewData.student.dob %>">
                </div>
                <div class="form-group">
                    <input type="number" name="score" id="score" class="form-control" placeholder="Score" value="<%- viewData.student.score %>">
                </div>
                <button type="submit" id="update-student" class="btn" style="background-color: #34495e; color: white;">Save Record</button>
            </div>
        </form>
        
    </div>
</body>
</html>
